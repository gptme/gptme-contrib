# Linear Integration Configuration
# Copy this to .env and fill in the values

# ===================
# REQUIRED VARIABLES
# ===================

# From Linear OAuth Application settings (Settings > API > OAuth Applications)
LINEAR_WEBHOOK_SECRET=your-webhook-signing-secret-here
LINEAR_CLIENT_ID=your-client-id-here
LINEAR_CLIENT_SECRET=your-client-secret-here

# Your ngrok URL + /oauth/callback (required for OAuth flow)
LINEAR_CALLBACK_URL=https://your-ngrok-domain.ngrok-free.app/oauth/callback

# Agent configuration (required, no defaults)
AGENT_NAME=myagent
AGENT_WORKSPACE=/home/user/myagent
DEFAULT_BRANCH=main  # Use 'master' if your repo uses master branch

# ===================
# OPTIONAL VARIABLES
# ===================

# Override derived paths (defaults are computed from AGENT_WORKSPACE)
# WORKTREE_BASE=/home/user/myagent-worktrees
# NOTIFICATIONS_DIR=/home/user/myagent/logs/linear-notifications

# Server configuration
# PORT=8081
